# No spec changes

The `ai-tagging` spec already contains the "Video-based suggestions via keyframes" requirement
and the `ai_tagging_use_video` config scenario (added in the Phase 2 delta, archived into
`openspec/specs/ai-tagging/spec.md`). This change is purely implementation — no requirement
or scenario updates are needed.
